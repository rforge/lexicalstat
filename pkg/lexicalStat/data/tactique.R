words.by.parts.by.slices.test <- list(
  slice1=list(part1=c("un", "deux"), part2=c("trois", "deux")),
  slice2=list(part1=c("trois", "quatre"), part2=c("quatre", "cinq")),
  slice3=list(part1=c("cinq", "six"), part2=c("trois", "quatre"))
);

words.by.parts.by.slices.test.2 <- list(
  slice1=list(part1=c("A", "B"), part2=c("a", "b"), part3=c("un", "deux")),
  slice2=list(part1=c("C", "D"), part2=c("c", "d"), part3=c("trois", "quatre")),
  slice3=list(part1=c("E", "F"), part2=c("e", "f"), part3=c("cinq", "six")),
  slice4=list(part1=c("un", "H"), part2=c("i", "j"), part3=c("sept", "A")),
  slice5=list(part1=c("I", "J"), part2=c("k", "l"), part3=c("neuf", "dix"))
);

words.by.parts.test <- list(
    part1=c("un", "deux", "trois"),
    part2=c("deux", "trois", "quatre")
);

words.by.slices.by.parts.test <- list(
  part1=list(slice1=c("un", "deux"), slice2=c("trois", "quatre")),
  part2=list(slice1=c("dix", "onze"), slice2=c("douze", "treize")),
  part3=list(slice1=c("A", "B"), slice2=c("C", "D"))
);

words.by.slices.by.parts.test2  <- list(
  partA1=list(
    partB1=c("word1", "word2"),
    partB2=c("word1", "word2")
    ),
  partA2=list(
    partB1=c("word1", "word2"),
    partB2=c("word1", "word2")
    ),
  partA3=list(
    partB3=c("word1", "word2"),
    partB4=c("word1", "word2")
    )
);


